@model Consultorio.Domain.Models.Cargo

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<fieldset>
    <h4>Confirma a exclusão do cargo @Html.DisplayFor(model => model.Descricao) ?</h4>
</fieldset>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(model=>model.EmpresaId)
    <p>
        <input type="submit" value="Excluir" class="btn btn-primary" />
        <input type="button" class="btn btn-primary" value="Cancelar" onclick="location.href='@Url.Action("Index", "Cargo", new { EmpresaId = Model.EmpresaId })'" />
    </p>
}
